name: Test with issue labels filter
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        continue-on-error: true
        with:
          github-repository-query: 'user:octocat'
          github-issue-labels: 'bug,enhancement'
          github-token: 'fake-github-token'
          notion-token: 'secret_fakefakefakefakefakefakefakefakefakefakefake'
          notion-database-id: 'aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee'
          delete-additional-rows: 'false'
          log-level: 'debug'
      - name: Assert action ran with issue labels (expected to fail with invalid tokens)
        run: |
          echo "Action outcome: ${{ steps.action.outcome }}"
          # The action is expected to fail with invalid API tokens.
          # We only verify it was invoked (not skipped).
          if [ "${{ steps.action.outcome }}" = "skipped" ]; then
            echo "FAIL: action was skipped, expected it to run"
            exit 1
          fi
          echo "PASS: action ran with github-issue-labels input"
